/**
 * GENERATED — do not edit by hand. Rewritten by `npm run media:import`.
 *
 * Maps Iron catalogue ids to bundled demonstration media. It ships EMPTY on
 * purpose: the artwork it is designed to point at is © Gym visual and is not
 * redistributable (see ./index.ts and scripts/import-exercise-media.mjs).
 */
import type { MediaEntry } from './types';

export const MEDIA: Readonly<Record<string, MediaEntry>> = {};

/** Who owns the artwork in MEDIA. Rendered under every frame that uses it. */
export const MEDIA_CREDIT: string | null = null;
